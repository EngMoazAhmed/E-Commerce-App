enum CheckoutType {
  delivery,
  address,
  payment,
  summary,
}

enum DeliveryType {
  standardDelivery,
  nextDayDelivery,
  nominatedDelivery,
}

enum AddressType {
  sameAddress,
  newAddress,
}

enum PaymentType {
  paypal,
  creditCard,
  crypto,
}
